# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @priority-lab/priority-lab`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t priority-lab-priority-lab`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system priority-lab-priority-lab && \
          adduser --system -G priority-lab-priority-lab priority-lab-priority-lab

COPY apps/priority-lab/dist priority-lab-priority-lab/
COPY apps/priority-lab/package.json priority-lab-priority-lab/
RUN chown -R priority-lab-priority-lab:priority-lab-priority-lab .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix priority-lab-priority-lab --omit=dev -f install

CMD [ "node", "priority-lab-priority-lab" ]
